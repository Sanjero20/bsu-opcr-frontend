import {
    AbsContainer,
    AccountsButton,
    CampusButton
} from "./Admin.styled";

const Admin = () => {
    return (
        <AbsContainer>
            <CampusButton></CampusButton>
            <AccountsButton></AccountsButton>
        </AbsContainer>
    );
};

export default Admin;